## 2. Add Design Fonts to the Next.js Layout

Using Pencil MCP, inspect the entire design and list every font family used.

Add all required fonts to:

```text
app/layout.tsx
```

Use `next/font/google` where applicable.

Reference pattern:

```typescript
import { Alumni_Sans } from "next/font/google";

const alumniSans = Alumni_Sans({
  variable: "--font-alumni-sans",
  subsets: ["latin"],
  weight: "400",
});
```

## 3. Convert color

convert all color relevant into variable, free to change values of the current css variables in theme to align with the intended UX/UI design, preserve the current variable names, only add new if neccessary and list it to markdown reported file.
